# Copy to .env at repo root and fill in your keys (never commit .env)

OPENROUTER_API_KEY=sk-or-your-key-here
OPENROUTER_MODEL=deepseek/deepseek-v4-flash
DATABASE_URL=sqlite+aiosqlite:///./data/app.db
UPLOAD_DIR=./data/uploads
MAX_FILE_SIZE_MB=50
AGENT_TIMEOUT_SEC=240
PYTHON_REPL_TIMEOUT_SEC=30

# MCP — local dev only (Horizon: set DATALENS_API_URL in horizon.prefect.io UI)
# DATALENS_API_URL=http://127.0.0.1:8000
# For Horizon + local backend: use ngrok HTTPS URL, e.g. https://abc123.ngrok-free.app
# DATALENS_JOB_TIMEOUT_SEC=600
# DATALENS_QUERY_TIMEOUT_SEC=600
